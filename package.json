{
  "name": "amazon-project-scraping",
  "version": "1.0.0",
  "description": "Extract clean, structured product data from Amazon - DOM-based scraper using Playwright, outputs labelled JSON per product.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cheerio": "^1.2.0",
    "playwright": "^1.60.0"
  }
}
